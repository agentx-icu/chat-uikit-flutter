/// toxee: moved to the common package so the message package (group sender
/// avatars) can resolve members too; re-exported here for existing importers.
export 'package:tencent_cloud_chat_common/utils/group_member_identity.dart';
